{
  "name": "family-spyfall",
  "version": "1.0.0",
  "description": "A real-time Spyfall game for families and friends",
  "private": true,
  "scripts": {
    "dev": "pnpm -C apps/server dev & pnpm -C apps/web dev",
    "dev:debug": "DEBUG=true pnpm -C apps/server dev & VITE_DEBUG=true pnpm -C apps/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "e2e": "pnpm -C apps/web test:e2e",
    "network-url": "./scripts/show-network-url.sh"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "eslint": "^8.55.0",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
